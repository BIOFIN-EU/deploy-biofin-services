services:
  auth-db:
    image: postgres:16.3
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${AUTH_POSTGRES_DB}
      - POSTGRES_USER=${AUTH_POSTGRES_USER}
      - POSTGRES_PASSWORD=${AUTH_POSTGRES_PASSWORD}
    volumes:
      - auth_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  auth-api:
    image: ghcr.io/biofin-eu/auth-api:latest
    restart: unless-stopped
    environment:
      - POSTGRES_HOST=auth-db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=${AUTH_POSTGRES_DB}
      - POSTGRES_USER=${AUTH_POSTGRES_USER}
      - POSTGRES_PASSWORD=${AUTH_POSTGRES_PASSWORD}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - JWT_ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_HOURS=24
      - AUTH_CLIENT_ID=${AUTH_CLIENT_ID}
      - AUTH_CLIENT_SECRET=${AUTH_CLIENT_SECRET}
      - GATEWAY_AUTH_CLIENT_ID=${GATEWAY_AUTH_CLIENT_ID}
      - GATEWAY_AUTH_CLIENT_SECRET=${GATEWAY_AUTH_CLIENT_SECRET}
      - PHYSICAL_AUTH_CLIENT_ID=${PHYSICAL_AUTH_CLIENT_ID}
      - PHYSICAL_AUTH_CLIENT_SECRET=${PHYSICAL_AUTH_CLIENT_SECRET}
    ports:
      - "8010:8010"
    depends_on:
      auth-db:
        condition: service_healthy

  physical-db:
    image: postgres:16.3
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${PHYSICAL_POSTGRES_DB}
      - POSTGRES_USER=${PHYSICAL_POSTGRES_USER}
      - POSTGRES_PASSWORD=${PHYSICAL_POSTGRES_PASSWORD}
    volumes:
      - physical_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  temporal-postgres:
    image: postgres:16.3
    restart: unless-stopped
    environment:
      - POSTGRES_DB=temporal
      - POSTGRES_USER=${TEMPORAL_POSTGRES_USER}
      - POSTGRES_PASSWORD=${TEMPORAL_POSTGRES_PASSWORD}
    volumes:
      - temporal_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  temporal:
    image: temporalio/auto-setup:latest
    restart: unless-stopped
    ports:
      - "7233:7233"
    environment:
      - DB=postgres12
      - DB_PORT=5432
      - POSTGRES_USER=${TEMPORAL_POSTGRES_USER}
      - POSTGRES_PWD=${TEMPORAL_POSTGRES_PASSWORD}
      - POSTGRES_SEEDS=temporal-postgres
      - TEMPORAL_LOG_LEVEL=warn
    depends_on:
      temporal-postgres:
        condition: service_healthy

  temporal-ui:
    image: temporalio/ui:latest
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_TASK_QUEUE=case-workflow-task-queue
    depends_on:
      temporal:
        condition: service_started

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      - MINIO_ENDPOINT=minio:9000
      - MINIO_BUCKET=case-documents
      - MINIO_SECURE=false
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  physical-api:
    image: ghcr.io/biofin-eu/physical-api:latest
    restart: unless-stopped
    command: uvicorn app.main:app --host 0.0.0.0 --port 8020 --reload
    ports:
      - "8020:8020"
    environment:
      - POSTGRES_HOST=physical-db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=${PHYSICAL_POSTGRES_DB}
      - POSTGRES_USER=${PHYSICAL_POSTGRES_USER}
      - POSTGRES_PASSWORD=${PHYSICAL_POSTGRES_PASSWORD}
      - AUTH_URL=http://auth-api:8010
      - AUTH_CLIENT_ID=${PHYSICAL_AUTH_CLIENT_ID}
      - AUTH_CLIENT_SECRET=${PHYSICAL_AUTH_CLIENT_SECRET}
      - WORKFLOW_DB_SCHEMA=workflow
      - CASE_DATA_DB_SCHEMA=case_data
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      - MINIO_ENDPOINT=minio:9000
      - MINIO_BUCKET=case-documents
      - MINIO_SECURE=false
    depends_on:
      physical-db:
        condition: service_healthy
      auth-api:
        condition: service_started
      temporal:
        condition: service_started
      minio:
        condition: service_healthy

  physical-worker:
    image: ghcr.io/biofin-eu/physical-api:latest
    restart: unless-stopped
    command: python -m app.workers.temporal_worker
    environment:
      - POSTGRES_HOST=physical-db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=${PHYSICAL_POSTGRES_DB}
      - POSTGRES_USER=${PHYSICAL_POSTGRES_USER}
      - POSTGRES_PASSWORD=${PHYSICAL_POSTGRES_PASSWORD}
      - AUTH_URL=http://auth-api:8010
      - AUTH_CLIENT_ID=${PHYSICAL_AUTH_CLIENT_ID}
      - AUTH_CLIENT_SECRET=${PHYSICAL_AUTH_CLIENT_SECRET}
      - WORKFLOW_DB_SCHEMA=workflow
      - CASE_DATA_DB_SCHEMA=case_data
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      - MINIO_ENDPOINT=minio:9000
      - MINIO_BUCKET=case-documents
      - MINIO_SECURE=false
    depends_on:
      physical-db:
        condition: service_healthy
      auth-api:
        condition: service_started
      temporal:
        condition: service_started
      minio:
        condition: service_healthy

  risk-db:
    image: postgres:16.3
    environment:
      POSTGRES_DB: ${RISK_POSTGRES_DB}
      POSTGRES_USER: ${RISK_POSTGRES_USER}
      POSTGRES_PASSWORD: ${RISK_POSTGRES_PASSWORD}
    volumes:
      - risk_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  risk-framework:
    image: ghcr.io/biofin-eu/risk-score-framework:latest
    restart: unless-stopped
    command: /service/run_server.sh
    environment:
      EE_PROJECT: ${RISK_EE_PROJECT}
      EE_SERVICE_ACCOUNT: ${RISK_EE_SERVICE_ACCOUNT}
      EE_KEY_FILE: /service/ee_secrets/ee-key.json
      LOG_LEVEL: ${LOG_LEVEL}
      DATABASE_HOST: risk-db
      DATABASE_PORT: 5432
      DATABASE_NAME: ${RISK_POSTGRES_DB}
      DATABASE_USER: ${RISK_POSTGRES_USER}
      DATABASE_PASS: ${RISK_POSTGRES_PASSWORD}
      WEB_PORT: ${RISK_WEB_PORT}
    depends_on:
      risk-db:
        condition: service_healthy
    volumes:
      - ./risk_data/ee_secrets/ee-key.json:/service/ee_secrets/ee-key.json
      - ./risk_data/data/cached_eu_polygons.json:/service/data/cached_eu_polygons.json
      - ./risk_data/data/HFP2009_rep.tif:/service/data/HFP2009_rep.tif
      - ./risk_data/data/Basic_Critical_Habitat_2025_rep.tif:/service/data/Basic_Critical_Habitat_2025_rep.tif
      - ./risk_data/data/wc2.1_30s_bioc_EC-Earth3-Veg_ssp245_2021-2040.tif:/service/data/wc2.1_30s_bioc_EC-Earth3-Veg_ssp245_2021-2040.tif
      - ./risk_data/data/wc2.1_30s_bioc_EC-Earth3-Veg_ssp245_2041-2060.tif:/service/data/wc2.1_30s_bioc_EC-Earth3-Veg_ssp245_2041-2060.tif
      - ./risk_data/data/wc2.1_30s_bioc_EC-Earth3-Veg_ssp585_2021-2040.tif:/service/data/wc2.1_30s_bioc_EC-Earth3-Veg_ssp585_2021-2040.tif
      - ./risk_data/data/wc2.1_30s_bioc_EC-Earth3-Veg_ssp585_2041-2060.tif:/service/data/wc2.1_30s_bioc_EC-Earth3-Veg_ssp585_2041-2060.tif
      - ./risk_data/data/WDPA_Mar2026_Public_shp:/service/data/WDPA_Mar2026_Public_shp
      - ./risk_data/data/countries:/service/data/countries
    # ports:
    #   - "${RISK_WEB_PORT}:${RISK_WEB_PORT}"


  gateway-api:
    image: ghcr.io/biofin-eu/gateway-api:latest
    restart: unless-stopped
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    environment:
      - AUTH_URL=http://auth-api:8010
      - PHYSICAL_API_URL=http://physical-api:8020
      - RISK_API_URL=http://risk-framework:${RISK_WEB_PORT}
      - AUTH_CLIENT_ID=${GATEWAY_AUTH_CLIENT_ID}
      - AUTH_CLIENT_SECRET=${GATEWAY_AUTH_CLIENT_SECRET}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - JWT_ALGORITHM=HS256
      - EXTRA_ALLOWED_HOSTS=${EXTRA_ALLOWED_HOSTS}
      - VIRTUAL_HOST=${VIRTUAL_HOST_GATEWAY}
      - LETSENCRYPT_HOST=${LETSENCRYPT_HOST_GATEWAY}
      - VIRTUAL_PORT=${VIRTUAL_PORT_GATEWAY}
      - HTTP_PROXY=${HTTP_PROXY}
      - HTTPS_PROXY=${HTTPS_PROXY}
      - http_proxy=${HTTP_PROXY}
      - https_proxy=${HTTPS_PROXY}
      - NO_PROXY=${DEFAULT_NO_PROXY}
      - no_proxy=${DEFAULT_NO_PROXY}
    depends_on:
      auth-api:
        condition: service_started
      physical-api:
        condition: service_started
      risk-framework:
        condition: service_started

  frontend-ui:
    image: ghcr.io/biofin-eu/frontend-ui:v1.0.0
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL}
      - EXTRA_ALLOWED_HOSTS=${EXTRA_ALLOWED_HOSTS}
      - VIRTUAL_HOST=${VIRTUAL_HOST_FRONTEND}
      - LETSENCRYPT_HOST=${LETSENCRYPT_HOST_FRONTEND}
      - VIRTUAL_PORT=${VIRTUAL_PORT_FRONTEND}
      - HTTP_PROXY=${HTTP_PROXY}
      - HTTPS_PROXY=${HTTPS_PROXY}
      - http_proxy=${HTTP_PROXY}
      - https_proxy=${HTTPS_PROXY}
      - NO_PROXY=${DEFAULT_NO_PROXY}
      - no_proxy=${DEFAULT_NO_PROXY}
    command: npm run dev -- --hostname 0.0.0.0 --port 3000
    depends_on:
      gateway-api:
        condition: service_started

volumes:
  risk_data:
  physical_data:
  temporal_pg_data:
  minio_data:
  auth_data:
